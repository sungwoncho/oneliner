<%= form_for(@oneline) do |f| %>
  <% if @oneline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oneline.errors.count, "error") %> prohibited this oneline from being saved:</h2>

      <ul>
      <% @oneline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
